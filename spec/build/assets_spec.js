describe("Assets", function() {

  describe("stylesheets", function() {

    /*it("should use default stylesheets array", function(done) {
      var uid = triggerBuild({
        enabledFormats: ["html"],
        stylesheets: ["spec/support/book/stylesheets/styles.scss", "spec/support/book/stylesheets/otherstyles.css"],
        layout: "spec/support/book/layouts/assets.html",
        success: function() {
          expect(buildContent(uid, "html/first-chapter.html")).toMatch("<link rel=\"stylesheet\" href=\"assets/styles.css\">");
          expect(buildContent(uid, "html/assets/styles.css")).toMatch("color: red;");
          expect(buildContent(uid, "html/first-chapter.html")).toMatch("<link rel=\"stylesheet\" href=\"assets/otherstyles.css\">");
          expect(buildContent(uid, "html/assets/otherstyles.css")).toMatch("color: green;");
          done();
        }
      });
    });

    it("should prioritize format stylesheets ")

    it('should bundle css files');
    it('should digest css files')*/

  });



});
